#include <stdio.h>
#include <stdlib.h>

int main(){
    printf("My %s is 211769727 \n","roll number is");            /* %s is used for strings*/
    printf("My roll number is %d \n", 21169727);                 /* %d is used for integers*/
    printf("The first few digits of pi are %f \n", 3.141592653); /* %f is used for floats (decimals)*/
    printf("%c comes after %c \n", 'B', 'A');                    /*%c is used for characters*/


    /*we can use different types of data is the same printf command by sperating them by commas according to 
    their order in the string of the printf command*/
}