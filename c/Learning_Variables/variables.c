#include <stdio.h>
#include <stdlib.h>

int main(){
    char name = 'John';
    int age = '50';

    printf("There one was a man names %s", name);
    printf("He was %d years old", age);
    printf("He really liked the name %s", name);
    printf("But he did not like that he was %d years old", age);
}