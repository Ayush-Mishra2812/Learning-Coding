#include <stdio.h>
#include <stdlib.h>

int main(){
    char name[] = "John";
    int age = 50;

    printf("There one was a man names %s \n", name);
    printf("He was %d years old \n", age);
    printf("He really liked the name %s \n", name);
    printf("But he did not like that he was %d years old \n", age);
}